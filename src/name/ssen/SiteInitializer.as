package name.ssen {
	import flash.display.Sprite;

	/**
	 * @author ssen(i@ssen.name)
	 */
	public class SiteInitializer {
		private var _index : Sprite;

		public function SiteInitializer(index : Sprite) {
			_index = index;
		}
	}
}
