package ssen.framework.widgets.text {
	import flashx.textLayout.elements.Configuration;

	/**
	 * @author ssen (i@ssen.name)
	 */
	public class MultilineConfig extends Configuration {
		public function MultilineConfig(initializeWithDefaults : Boolean = true) {
			super(initializeWithDefaults);
		}
	}
}
